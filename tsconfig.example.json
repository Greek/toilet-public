{
  "extends": "ts-node/node12/tsconfig.json",

  "ts-node": {
    // this is pointless because there's no typechecking anymore,
    // but i'd rather sacrifice that over speed.
    "transpileOnly": true,

    "files": true,

    "compilerOptions": {}
  },
  "compilerOptions": {}
}
